# ─── BizChat Frontend — Environment Variables ─────────────────────────────────
# Copy to .env.local for local dev, or add in Vercel Dashboard → Settings → Env Vars

# Backend URL. Leave empty for local dev (Vite proxy handles it).
# In production: the backend is accessed via 52.66.154.194:3000
VITE_API_URL=
