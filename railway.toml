[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile"

[deploy]
startCommand = "node --max-old-space-size=256 dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
